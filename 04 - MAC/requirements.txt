# VEO Pro Max - Dependencies (macOS)
# Version: 2.3.9

# UI Framework
# ★ 6.6.0+ required for stable Apple Silicon (M1/M2/M3) support
PySide6>=6.6.0

# Browser Automation
playwright>=1.40.0

# HTTP Client
requests>=2.31.0

# Async HTTP (WebSocket bridge, concurrent operations)
aiohttp>=3.9.0

# Encryption (AES-256 for license + API keys)
cryptography>=41.0.0

# WebSocket (Extension ↔ App bridge)
websockets>=12.0

# Image Processing (upscaler, thumbnail, pipeline)
Pillow>=10.0.0
numpy>=1.24.0
# ★ MUST use headless variant on macOS — opencv-python loads its own
#   Qt framework which conflicts with PySide6 → segfault on startup
opencv-python-headless>=4.8.0

# Process Management (Chrome lifecycle, system monitoring)
psutil>=5.9.0

# Document Reading (Import .docx files)
python-docx>=1.0.0

# SSL Certificate Verification (fix SSL errors on some systems)
certifi>=2023.0.0

# ── Optional: AI Upscaler (install separately if needed) ──
# torch>=2.0.0
# realesrgan>=0.3.0
# basicsr>=1.4.2
# gfpgan>=1.3.8
